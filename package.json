{
  "name": "basic-carousal-app",
  "version": "1.0.0",
  "description": "A basic LightningJS (Blits) app with Home, Movies and Shows pages, a hero carousel and rails of cards.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "dependencies": {
    "@lightningjs/blits": "^1.20.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.4.0"
  }
}
